script(src="catalog.js")
    include includes/head.pug
P Добро пожаловать! #{account.name}
ul
    each product in products
        li
            p=product.id
            h1=product.name
            p=product.price
            p=product.inStock
            p=product.description 
            h2=product.weight
            h2=product.flavor
            button(onclick=`addToCart("${product.id}")`) Добавить в карзину
